
services:
  redis-cache:
    image: redis:6.2-alpine
    container_name: redis-cache
    ports:
      - "6379:6379"
    networks:
      - oem-network
    command: redis-server --save 60 1 --loglevel warning
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 5s
      timeout: 3s
      retries: 5



  # 1. CSDL MYSQL
  mysql-db:
    image: mysql:8.0
    container_name: mysql-db
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_ROOT_HOST: '%'
      MYSQL_DATABASE: sharedDB # Dữ liệu này chỉ là DB ban đầu Docker tạo
    ports:
      - "8080:3306"
    networks:
      - oem-network
    volumes:
      - db_data:/var/lib/mysql

    # 🌟 BỔ SUNG QUAN TRỌNG: Healthcheck
    healthcheck:
      test: [ "CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p1234" ]
      interval: 5s
      timeout: 10s
      retries: 5
      start_period: 20s

  # 2. USER-SERVICE
  user-service:
    build:
      context: .
      dockerfile: ./user-service/Dockerfile
    container_name: user-service
    ports:
      - "8001:8080"
    networks:
      - oem-network
    environment:
      JWT_SECRET: "we_dont_talk_anymore_like_we_used_to_do"
      INTERNAL_SERVICE_SECRET: "we_dont_talk_anymore"
    depends_on:
      mysql-db:
        condition: service_healthy
    command: /bin/sh -c "sleep 15 && java -jar /app.jar"

  # 3. VEHICLE-SERVICE
  vehicle-service:
    build:
      context: .
      dockerfile: ./vehicle-service/Dockerfile
    container_name: vehicle-service
    ports:
      - "8002:8080"
    networks:
      - oem-network
    environment:
      INTERNAL_SERVICE_SECRET: "we_dont_talk_anymore"
    depends_on:
      mysql-db:
        condition: service_healthy
    command: /bin/sh -c "sleep 15 && java -jar /app.jar"

  # 4. PART-SERVICE (Đã thêm depends_on)
  part-service:
    build:
      context: . # Sửa thành thư mục gốc
      dockerfile: ./part-service/Dockerfile # Chỉ định rõ Dockerfile cần dùng
    container_name: part-service
    ports:
      - "8004:8080"
    networks:
      - oem-network
    environment:
      INTERNAL_SERVICE_SECRET: "we_dont_talk_anymore"
      SPRING_DATA_REDIS_HOST: redis-cache
      SPRING_DATA_REDIS_PORT: 6379
    depends_on:
      redis-cache:
        condition: service_started
      mysql-db:
        condition: service_healthy
    command: /bin/sh -c "sleep 15 && java -jar /app.jar"

  # 5. CAMPAIGN-SERVICE (Đã sửa lỗi cú pháp và depends_on)
  campaign-service:
    build:
      context: . # Sửa thành thư mục gốc
      dockerfile: ./campaign-service/Dockerfile # Chỉ định rõ Dockerfile cần dùng
    container_name: campaign-service
    ports:
      - "8003:8080"
    networks:
      - oem-network
    environment:
      INTERNAL_SERVICE_SECRET: "we_dont_talk_anymore"
    depends_on:
      mysql-db:
        condition: service_healthy
    command: /bin/sh -c "sleep 15 && java -jar /app.jar"

  # 6. WARRANTY-SERVICE (ĐÃ THÊM depends_on CHÍNH XÁC)
  warranty-service:
    build:
      context: .
      dockerfile: ./warranty-service/Dockerfile
    container_name: warranty-service
    ports:
      - "8005:8080"
    networks:
      - oem-network
    volumes:
      - ./uploads:/app/uploads
    command: /bin/sh -c "sleep 15 && java -jar /app.jar"
    # Khai báo phụ thuộc cho WARRANTY-SERVICE
    environment:
      INTERNAL_SERVICE_SECRET: "we_dont_talk_anymore"
    depends_on:
      mysql-db:
        condition: service_healthy
      user-service:
        condition: service_started
      vehicle-service:
        condition: service_started
      part-service:
        condition: service_started
      campaign-service:
        condition: service_started
      # Đảm bảo tất cả chạy trước khi Warranty Service khởi động

  api-gateway:
    build:
      context: .
      dockerfile: ./api-gateway/Dockerfile
    container_name: api-gateway
    ports:
      - "80:8080"
    networks:
      - oem-network
    environment:
      JWT_SECRET: "we_dont_talk_anymore_like_we_used_to_do"
      INTERNAL_SERVICE_SECRET: "we_dont_talk_anymore"
    command: /bin/sh -c "sleep 15 && java -jar /app.jar"
    depends_on:
      user-service:
        condition: service_started
      vehicle-service:
        condition: service_started
      part-service:
        condition: service_started
      campaign-service:
        condition: service_started
      warranty-service:
        condition: service_started

networks:
  oem-network:
    driver: bridge

volumes:
  db_data: